# Results Layout

- `artifacts/`: serialized experiment objects (`.pkl`).
- `metrics/`: aggregated experiment tables (`.csv`).
- `figures/`: plots and charts (`.png`).
- `logs/`: runner logs (`.log`).
